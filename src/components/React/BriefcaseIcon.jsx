import { Briefcase } from "lucide-react"; // O cualquier librería de íconos que uses

const BriefcaseIcon = ({ className }) => {
  return <Briefcase className={className} />;
};

export default BriefcaseIcon;
